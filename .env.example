# Google Sheets / Drive (OAuth2)
# credentials.json を本ファイルと同じディレクトリに配置
# npm run auth で token.json を生成

# WordPress REST API
WP_URL=https://example.com
WP_USER=your-username
WP_APP_PASSWORD=your-application-password

# YouTube Data API (OAuth2)
# workflow/ に token.pickle と credentials.json を配置
# 1Password から取得
